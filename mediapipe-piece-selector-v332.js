/* HappyHolo V3.32.3 — MediaPipe MagicTouch interactive machine part selector */
(() => {
  'use strict';
  const LIB='https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/+esm';
  const WASM='https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/wasm';
  const MODEL='https://storage.googleapis.com/mediapipe-models/interactive_segmenter_v2/magic_touch/int8/1/interactive_segmentation.task';
  let apiPromise=null,segmenterPromise=null;
  const plan=()=>Array.isArray(window.happyHoloSelectionPlan)?window.happyHoloSelectionPlan:null;
  const cloneImageData=m=>new ImageData(new Uint8ClampedArray(m.data),m.width,m.height);
  async function getApi(){apiPromise ||= import(LIB);return apiPromise;}
  async function getSegmenter(setStatus){if(!segmenterPromise){segmenterPromise=(async()=>{setStatus?.('Chargement MediaPipe MagicTouch…');const api=await getApi();const vision=await api.FilesetResolver.forVisionTasks(WASM);setStatus?.('Chargement du modèle MagicTouch…');return api.InteractiveSegmenter.createFromOptions(vision,{baseOptions:{modelAssetPath:MODEL,delegate:'CPU'}});})();}return segmenterPromise;}
  function fileToDataURL(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file);});}
  function maskToImageData(mask,targetW,targetH,baseMask,threshold){const srcW=mask.width,srcH=mask.height,values=mask.getAsFloat32Array();const src=document.createElement('canvas');src.width=srcW;src.height=srcH;const sctx=src.getContext('2d',{willReadFrequently:true}),out=sctx.createImageData(srcW,srcH),t=Number(threshold)||.45;for(let i=0;i<srcW*srcH;i++){const a=values[i]>=t?Math.min(255,Math.max(0,Math.round(values[i]*255))):0,o=i*4;out.data[o]=0;out.data[o+1]=200;out.data[o+2]=255;out.data[o+3]=a;}sctx.putImageData(out,0,0);const dst=document.createElement('canvas');dst.width=targetW;dst.height=targetH;const dctx=dst.getContext('2d',{willReadFrequently:true});dctx.imageSmoothingEnabled=false;dctx.drawImage(src,0,0,targetW,targetH);const converted=dctx.getImageData(0,0,targetW,targetH);if(baseMask?.data){for(let i=0;i<targetW*targetH;i++){const o=i*4;converted.data[o+3]=Math.min(converted.data[o+3],baseMask.data[o+3]||0);}}return converted;}
  function addSelection(mask){const selections=plan();if(!selections)return;const mode=window.HappyHoloExplodeViewState?.mode||'simple';const existing=selections.filter(s=>s?.source==='mediapipe-magic-touch').length;selections.push({id:Date.now()+Math.random(),name:`Pièce IA ${existing+1}`,depth:.34+existing*.025,action:'explodeview',intensity:65,timing:'all',actionZones:[],explodeOrder:existing+1,explodeDirection:'auto',explodeMode:mode,source:'mediapipe-magic-touch',confidence:1,mask,initialMask:cloneImageData(mask)});window.dispatchEvent(new CustomEvent('happyholo:selection-plan',{detail:{count:selections.length,source:'mediapipe-magic-touch'}}));window.dispatchEvent(new CustomEvent('happyholo-action-plan-changed'));}
  async function open(){
    const selections=plan(),file=document.getElementById('file')?.files?.[0];if(!file){alert('Choisis d’abord la photo de la machine.');return;}if(!selections?.[0]?.mask){alert('Crée d’abord le relief pour obtenir le détourage global de la machine.');return;}const base=selections[0].mask;
    const originalSource=window.HappyHoloReliefState?.sourceImg||null;
    const overlay=document.createElement('div');overlay.style.cssText='position:fixed;inset:0;z-index:10000150;background:#05080bf5;color:#fff;display:flex;flex-direction:column;padding:max(10px,env(safe-area-inset-top)) 10px max(10px,env(safe-area-inset-bottom));box-sizing:border-box;font-family:system-ui,sans-serif';
    const head=document.createElement('div');head.style.cssText='display:flex;justify-content:space-between;gap:8px;align-items:flex-start;margin-bottom:8px';const title=document.createElement('div');title.innerHTML='<b style="font-size:19px">Magic Touch — sélection d’une pièce</b><div style="font-size:12px;color:#c7d7e3;margin-top:3px">Vert = ajouter · Rouge = retirer · puis Valider la pièce.</div>';const finish=document.createElement('button');finish.textContent='Terminer';finish.style.cssText='margin:0;background:#eee;color:#111;border:0;border-radius:10px;padding:10px 14px;font-weight:900';head.append(title,finish);
    const controls=document.createElement('div');controls.style.cssText='display:flex;gap:7px;flex-wrap:wrap;margin-bottom:8px';const mk=(txt,bg)=>{const b=document.createElement('button');b.textContent=txt;b.style.cssText=`margin:0;border:0;border-radius:10px;padding:10px 12px;font-weight:900;background:${bg};color:#111`;return b};const plus=mk('➕ Ajouter','#76e59a'),minus=mk('➖ Retirer','#ff8f8f'),reset=mk('↺ Refaire pièce','#ddd'),validate=mk('✓ Valider la pièce','#62c7ff');controls.append(plus,minus,reset,validate);
    const thresholdWrap=document.createElement('label');thresholdWrap.style.cssText='display:flex;align-items:center;gap:8px;font-size:12px;margin:0 0 8px;color:#d9e5ed';thresholdWrap.innerHTML='Précision du bord <input type="range" min="25" max="75" value="45" step="5" style="flex:1"><b>45%</b>';const threshold=thresholdWrap.querySelector('input'),thresholdOut=thresholdWrap.querySelector('b');
    const status=document.createElement('div');status.textContent='Récupération de la photo originale…';status.style.cssText='padding:9px 11px;border-radius:10px;background:#142531;color:#dce9f2;font-size:12px;margin-bottom:8px';const stage=document.createElement('div');stage.style.cssText='position:relative;flex:1;min-height:260px;overflow:hidden;border:2px solid #28a8ee;border-radius:14px;background:#111;touch-action:none';
    const sourceCanvas=document.createElement('canvas'),maskCanvas=document.createElement('canvas'),strokeCanvas=document.createElement('canvas');for(const c of [sourceCanvas,maskCanvas,strokeCanvas])c.style.cssText='position:absolute;pointer-events:none;display:block';maskCanvas.style.opacity='.62';stage.append(sourceCanvas,maskCanvas,strokeCanvas);overlay.append(head,controls,thresholdWrap,status,stage);document.body.appendChild(overlay);finish.onclick=()=>overlay.remove();
    sourceCanvas.width=maskCanvas.width=strokeCanvas.width=base.width;sourceCanvas.height=maskCanvas.height=strokeCanvas.height=base.height;const sourceCtx=sourceCanvas.getContext('2d',{alpha:false});sourceCtx.fillStyle='#111';sourceCtx.fillRect(0,0,base.width,base.height);
    let sourceOK=false;
    try{if(originalSource?.naturalWidth&&originalSource?.naturalHeight){sourceCtx.drawImage(originalSource,0,0,base.width,base.height);sourceOK=true;}}catch(e){console.warn('[MagicTouch original source]',e);}
    if(!sourceOK){try{const img=new Image();img.decoding='sync';img.src=await fileToDataURL(file);await new Promise((res,rej)=>{img.onload=res;img.onerror=rej;});sourceCtx.drawImage(img,0,0,base.width,base.height);sourceOK=true;}catch(e){console.error('[MagicTouch file fallback]',e);}}
    if(!sourceOK){status.textContent='Impossible de récupérer la photo originale. Ferme MagicTouch et recrée le relief.';return;}
    function syncLayout(){const r=stage.getBoundingClientRect(),aspect=base.width/base.height;let dw=r.width,dh=dw/aspect;if(dh>r.height){dh=r.height;dw=dh*aspect;}const left=(r.width-dw)/2,top=(r.height-dh)/2;for(const c of [sourceCanvas,maskCanvas,strokeCanvas]){c.style.left=left+'px';c.style.top=top+'px';c.style.width=dw+'px';c.style.height=dh+'px';}}syncLayout();window.addEventListener('resize',syncLayout,{passive:true});
    let brush='positive',strokes=[],currentMask=null,busy=false,pointerDown=false,currentPoints=[];const setBrush=v=>{brush=v;plus.style.outline=v==='positive'?'3px solid #fff':'none';minus.style.outline=v==='negative'?'3px solid #fff':'none';status.textContent=v==='positive'?'Mode AJOUTER : touche ou trace dans la pièce.':'Mode RETIRER : touche ou trace sur ce qui ne doit pas être sélectionné.';};plus.onclick=()=>setBrush('positive');minus.onclick=()=>setBrush('negative');setBrush('positive');
    let segmenter;try{segmenter=await getSegmenter(t=>status.textContent=t);status.textContent='Analyse de la photo originale…';segmenter.setImage(sourceCanvas);status.textContent='Photo originale prête. Mode AJOUTER : touche la roue, selle, phare, moteur…';}catch(e){console.error('[MagicTouch init]',e);status.textContent='MediaPipe indisponible : '+(e?.message||e);return;}
    function containPoint(ev){const r=sourceCanvas.getBoundingClientRect(),px=ev.clientX-r.left,py=ev.clientY-r.top;if(px<0||py<0||px>r.width||py>r.height)return null;return{x:px/r.width,y:py/r.height};}
    function drawStrokes(){const c=strokeCanvas.getContext('2d');c.clearRect(0,0,strokeCanvas.width,strokeCanvas.height);const all=currentPoints.length?[...strokes,{brushMode:brush,point:currentPoints,isCompleted:false}]:strokes;for(const s of all){if(!s.point?.length)continue;c.save();c.strokeStyle=s.brushMode==='negative'?'#ff3b30':'#34c759';c.fillStyle=c.strokeStyle;c.lineWidth=Math.max(4,base.width/180);c.lineCap='round';c.lineJoin='round';if(s.point.length===1){const p=s.point[0];c.beginPath();c.arc(p.x*c.canvas.width,p.y*c.canvas.height,Math.max(7,base.width/140),0,Math.PI*2);c.fill();c.strokeStyle='#fff';c.lineWidth=2;c.stroke();}else{c.beginPath();c.moveTo(s.point[0].x*c.canvas.width,s.point[0].y*c.canvas.height);for(let i=1;i<s.point.length;i++)c.lineTo(s.point[i].x*c.canvas.width,s.point[i].y*c.canvas.height);c.stroke();}c.restore();}}
    async function recompute(){if(busy||!strokes.length)return;busy=true;validate.disabled=true;try{status.textContent='Calcul précis des limites de la pièce…';const api=await getApi();const formatted=strokes.map(s=>({brushMode:s.brushMode==='negative'?(api.BrushMode?.NEGATIVE??2):(api.BrushMode?.POSITIVE??1),point:s.point,isCompleted:true}));const result=segmenter.segment(formatted),mpMask=result?.confidenceMasks?.[0]||result?.categoryMask||result;if(!mpMask?.getAsFloat32Array)throw new Error('Masque MediaPipe non reçu');currentMask=maskToImageData(mpMask,base.width,base.height,base,Number(threshold.value)/100);const c=maskCanvas.getContext('2d');c.clearRect(0,0,base.width,base.height);c.putImageData(currentMask,0,0);status.textContent='Sélection proposée. Corrige avec Ajouter/Retirer, puis Valider la pièce.';}catch(e){console.error('[MagicTouch segment]',e);status.textContent='Erreur de segmentation : '+(e?.message||e);}finally{busy=false;validate.disabled=false;}}
    stage.addEventListener('pointerdown',ev=>{if(busy)return;const p=containPoint(ev);if(!p)return;pointerDown=true;currentPoints=[p];stage.setPointerCapture?.(ev.pointerId);drawStrokes();});stage.addEventListener('pointermove',ev=>{if(!pointerDown)return;const p=containPoint(ev);if(!p)return;const last=currentPoints[currentPoints.length-1];if(!last||Math.hypot(p.x-last.x,p.y-last.y)>.008){currentPoints.push(p);drawStrokes();}});const endPointer=async ev=>{if(!pointerDown)return;pointerDown=false;if(currentPoints.length){strokes.push({brushMode:brush,point:[...currentPoints],isCompleted:true});currentPoints=[];drawStrokes();await recompute();}try{stage.releasePointerCapture?.(ev.pointerId)}catch(_){}};stage.addEventListener('pointerup',endPointer);stage.addEventListener('pointercancel',endPointer);
    reset.onclick=()=>{strokes=[];currentPoints=[];currentMask=null;maskCanvas.getContext('2d').clearRect(0,0,base.width,base.height);drawStrokes();setBrush('positive');status.textContent='Pièce effacée. Touche à nouveau une zone à conserver.';};threshold.oninput=()=>{thresholdOut.textContent=threshold.value+'%';if(strokes.length)recompute();};validate.onclick=()=>{if(!currentMask){status.textContent='Sélectionne d’abord une pièce.';return;}addSelection(cloneImageData(currentMask));strokes=[];currentPoints=[];currentMask=null;maskCanvas.getContext('2d').clearRect(0,0,base.width,base.height);drawStrokes();setBrush('positive');status.textContent='Pièce validée. Touche maintenant la pièce suivante.';};
  }
  window.HappyHoloMagicTouch={open};window.dispatchEvent(new CustomEvent('happyholo-magictouch-ready'));console.log('[HAPPYHOLO] MediaPipe MagicTouch V3.32.3 chargé');
})();