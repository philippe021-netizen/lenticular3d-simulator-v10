(()=>{'use strict';function init(){const dock=document.getElementById('hhProgressDock'),txt=document.getElementById('hhProgressText'),pct=document.getElementById('hhProgressPct'),bar=document.getElementById('hhProgressBar');if(!dock||!txt||!pct||!bar)return;let hideTimer=null;function show(label,p){clearTimeout(hideTimer);dock.classList.add('show');txt.textContent=label||'Calcul en cours…';if(Number.isFinite(p)){const v=Math.max(0,Math.min(100,Math.round(p)));pct.textContent=v+' %';bar.classList.remove('indeterminate');bar.style.width=v+'%';bar.style.transform='none'}else{pct.textContent='';bar.style.width='';bar.style.transform='';bar.classList.add('indeterminate')}}function done(label='Terminé'){show(label,100);hideTimer=setTimeout(()=>dock.classList.remove('show'),1300)}function parse(raw){const s=String(raw||'').replace(/\s+/g,' ').trim();if(!s)return;const m=s.match(/(\d{1,3})\s*%/);if(m){show(s,Math.min(100,+m[1]));return}const low=s.toLowerCase();if(/erreur|impossible|indisponible/.test(low)){show(s,0);hideTimer=setTimeout(()=>dock.classList.remove('show'),3500);return}if(/termin|prêt|créées|creees|recalculé|recalcule|fond créé|fond cree|générée|generee/.test(low)&&!/création|creation/.test(low)){done(s);return}if(/détour|detour|reconstruction|tiefling|analyse|recalcul|création|creation|génération|generation|traitement|chargement|préparation|preparation/.test(low))show(s,null)}function watch(id){const e=document.getElementById(id);if(!e)return;parse(e.textContent);new MutationObserver(()=>parse(e.textContent)).observe(e,{subtree:true,childList:true,characterData:true})}watch('status');watch('aiStatus');[['analyze','Détourage et analyse…'],['calcSubject','Recalcul du sujet…'],['calcBg','Recalcul du fond…'],['views','Création des 9 vues…'],['aiGenerate','Génération du fond IA…'],['zip','Préparation du ZIP…']].forEach(([id,label])=>{const e=document.getElementById(id);if(e)e.addEventListener('click',()=>show(label,null),true)})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();