name: Build ZipDepth ONNX
on:
  workflow_dispatch:
  push:
    branches:
      - experiment/zipdepth
    paths:
      - '.github/workflows/build-zipdepth-onnx.yml'
permissions:
  contents: write
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experiment/zipdepth
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Clone ZipDepth
        run: git clone --depth 1 https://github.com/fabiotosi92/ZipDepth.git /tmp/ZipDepth
      - name: Install exporter
        working-directory: /tmp/ZipDepth
        run: |
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install onnx onnxsim onnxscript
          pip install -r requirements.txt
          pip install -e .
      - name: Export NPU/mobile ONNX 384
        working-directory: /tmp/ZipDepth
        run: |
          python scripts/export.py --ckpt checkpoints/zipdepth_base_npu.pth --format onnx --height 256 --width 256 --npu --output zipdepth_base_npu_256.onnx
          cp zipdepth_base_npu_256.onnx "$GITHUB_WORKSPACE/zipdepth_base_npu_256.onnx"
      - name: Commit model only to experiment branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f zipdepth_base_npu_256.onnx
          git commit -m "model: add ZipDepth NPU ONNX 384" || exit 0
          git push origin HEAD:experiment/zipdepth
