(()=>{'use strict';
const previousWrite=document.write.bind(document);
function safeBgSource(){return `function warpBg(outW,outH,t,amp){const src=cv(bg).getContext('2d',{willReadFrequently:true}).getImageData(0,0,bg.naturalWidth,bg.naturalHeight),sw=src.width,sh=src.height,out=new ImageData(outW,outH),od=out.data,scale=Math.max(outW/sw,outH/sh)*1.20,ox=(outW-sw*scale)/2,oy=(outH-sh*scale)/2,str=+$('bgStrength').value/100,drift=+$('bgDrift').value/100,groundLock=Math.max(.60,Math.min(1,+($('bgGroundLock')?.value||96)/100)),safeT=t*(1-.22*Math.pow(Math.abs(t),1.55)),subjectShift=safeT*amp*(+$('subjectMove').value/100),horizonY=outH*.54,horizonShift=-safeT*amp*drift*.12,maxShift=Math.max(2,amp*.58);for(let y=0;y<outH;y++){const syRaw=(y-oy)/scale,sy=Math.max(0,Math.min(sh-1,syRaw)),gb=smooth01(horizonY,outH*.93,y),gshift=mix(horizonShift,subjectShift*.55,gb);for(let x=0;x<outW;x++){const sxRaw=(x-ox)/scale,sx0=Math.max(0,Math.min(sw-1,sxRaw)),z=sample(bDepth.data,bDepth.width,bDepth.height,sx0/(sw-1)*(bDepth.width-1),sy/(sh-1)*(bDepth.height-1),0)/255,pz=z-.5,depthShift=-safeT*amp*drift*.12+safeT*amp*str*pz*.72;let shift=mix(depthShift,gshift,gb*groundLock);shift=Math.max(-maxShift,Math.min(maxShift,shift));const sx=Math.max(0,Math.min(sw-1,sx0-shift/scale)),p=(y*outW+x)*4;for(let c=0;c<4;c++)od[p+c]=sample(src.data,sw,sh,sx,sy,c)}}return out}`}
function patch(html){if(typeof html!=='string'||!html.includes('function warpBg(outW,outH,t,amp)'))return html;const re=/function warpBg\(outW,outH,t,amp\)\{[\s\S]*?\}function subjectLayer\(outW,outH,t,amp\)\{/;if(!re.test(html))return html;html=html.replace(re,safeBgSource()+"function subjectLayer(outW,outH,t,amp){");html=html.replace('Tiefling V21','Tiefling V321 — fond sécurisé');html=html.replace('Une seule photo : détourage automatique du sujet, reconstruction du fond, profondeur Tiefling, sujet pivoté et sol protégé.','V321 test : sujet inchangé, fond à parallaxe plafonnée, marges élargies et bords sécurisés.');return html}
document.write=function(...args){return previousWrite(...args.map(patch))};
})();
